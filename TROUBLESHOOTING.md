# 🚨 Troubleshooting Guide - استكشاف الأخطاء

## ❌ المشاكل الشائعة والحلول

### 1️⃣ Network Error عند محاولة Login

**المشكلة:**
```
Network Error
البريد الإلكتروني وكلمة المرور مطلوبان
```

**الأسباب المحتملة:**
- Backend server غير مشغل
- Frontend يشير إلى URL خاطئة
- محظوظ CORS

**الحل:**

#### أ) تحقق من Backend
```bash
# يجب أن يستجيب
curl http://192.168.1.6:5000/api/health

# إذا لم يستجيب:
cd backend
npm start
```

#### ب) تحقق من Frontend env
```bash
# في frontend/.env.local
VITE_API_URL=http://192.168.1.6:5000/api  # ✅ فقط /api
```

#### ج) امسح الـ Browser Cache
```
Chrome: Ctrl + Shift + R
Firefox: Ctrl + Shift + F5
Safari: Cmd + Shift + R
```

#### د) افتح Browser Console (F12)
```
انظر للـ Network tab
يجب أن ترى:
- GET /api/health → 200 OK
- POST /api/auth/login → 200 or 401
```

---

### 2️⃣ صور السيارات لا تظهر

**الحل:**
```bash
# تحقق من وجود الصور
ls frontend/public/images/cars/

# يجب أن تكون 54 صورة موجودة
```

---

### 3️⃣ بريد التحقق لم يصل

**المشكلة:** لم أتلقى بريد تحقق بعد التسجيل

**الحل:**
```bash
# تحقق من بيانات البريد في backend/.env
EMAIL_USER=ngtr613@gmail.com
EMAIL_APP_PASSWORD=lbnuegpnxzuvenji

# قد تحتاج إلى:
# 1. تحديث Gmail App Password
# 2. تفعيل "Less secure apps"
# 3. تفعيل 2FA
```

---

### 4️⃣ خطأ "بيانات دخول غير صحيحة" 

**المشكلة:** حتى مع كلمة مرور صحيحة، لا أستطيع تسجيل الدخول

**السبب:** البريد الإلكتروني غير موثق

**الحل:**
1. اذهب إلى `/signup`
2. سجل حساب جديد
3. تحقق من بريدك (تحقق من Spam)
4. أدخل رمز التحقق
5. حاول تسجيل الدخول مرة أخرى

---

### 5️⃣ Blank Screen أو Loading Forever

**السبب:** قاعدة البيانات لا تتصل

**الحل:**
```bash
# تحقق من MongoDB Atlas
1. اذهب إلى https://cloud.mongodb.com
2. تأكد من أن cluster مشغل (green ✅)
3. تحقق من IP whitelist: Network Access
4. يجب أن يكون 0.0.0.0/0 مسموح أو أضيف Vercel IPs

# أعد تشغيل Backend
cd backend && npm start
```

---

### 6️⃣ بطء شديد أو timeout

**السبب:** اتصال قاعدة البيانات بطيء

**الحل:**
```bash
# 1. تحقق من اتصالك بـ الإنترنت
# 2. تأكد من قرب MongoDB cluster منك
# 3. و بدل DATABASE_URL
```

---

## 🔧 أدوات Debugging

### Browser DevTools
```
F12 → Network tab
- شاهد requests و responses
- تحقق من CORS headers
- تحقق من status codes (200, 400, 401, 500)
```

### Backend Logs
```bash
cd backend && npm start
# شاهد الأخطاء في الـ console
```

### MongoDB Atlas
```
https://cloud.mongodb.com
- انظر إلى Activity
- Check Metrics
- Review Logs
```

---

## ✅ قائمة تحقق قبل الإنتاج

- [ ] Backend يعمل بدون أخطاء
- [ ] Frontend يحمل بسرعة
- [ ] Login/Signup يعمل
- [ ] البريد الإلكتروني يصل
- [ ] السيارات تظهر
- [ ] QR code ظاهر في checkout
- [ ] لا توجد أخطاء في console (F12)
- [ ] جميع الصور تحمل

---

## 📞 Contact Support

إذا واجهت مشكلة أخرى:

1. تحقق من Browser Console (F12)
2. انسخ رسالة الخطأ
3. شارك:
   - الـ URL
   - الـ Steps to reproduce
   - شاشة الخطأ
   - Browser info

---

